Compilación:
	Opción 1) Correr el archivo Makefile usando el comando make.
	Opción 2) flex -Cfa tpe1.l
		  gcc -o tpe1 lex.yy.c -lfl

Ejecución:
	Colocar en el archivo input.txt ubicado en el mismo directorio que el programa la entrada del mismo y correrlo.
	La salida del programa podrá verse en el archivo output.txt ubicado en el mismo directorio que el programa .
